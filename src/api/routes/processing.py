# from src.services import generate_company_domains
#
#
# def start_processing(property_details: str, compose_email_prompt: str):
#     company_domains: list[str] = generate_company_domains(property_details)
#     for company_domain in company_domain:
#         emails = extract_domain()
#         for email in emails:
#             compose_email = compose_email(email, client_email)
